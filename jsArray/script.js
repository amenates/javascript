// массив - это упорядоченный набор данных

let m = []; // объявляем пустой массив
let m1 = [99, 33, 'hello']; // объявляем не пустой массив
// индекс это порядок элементов внутри массива, например у 99 индекс 0, а у 'hello' - 2

// обратиться к индексу массива:
console.log(m1[2]);
// m1 имя массива 2 - индекс массива

